# vCFO

Monorepo scaffold for a Virtual CFO platform.

## Structure
- `apps/api`: Fastify + Prisma backend
- `apps/web`: Next.js frontend
- `packages/*`: shared libraries and contracts
- `infra/*`: Docker and K8s manifests

## Getting started
This is a skeleton based on `repo blueprint for Cursor.md`.
